
import 'package:flutter/material.dart';

class CartSliverProducts extends StatelessWidget {
  const CartSliverProducts({super.key});

  @override
  Widget build(BuildContext context) {
    
    return const Placeholder();
  }
}