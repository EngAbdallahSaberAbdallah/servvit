import 'package:flutter/material.dart';

class RequestsTabView extends StatelessWidget {
  const RequestsTabView({super.key});

  @override
  Widget build(BuildContext context) {
    //! call get allrequests
    return Container(
      child: Center(
        child: Text('Requests will be here'),
      ),
    );
  }
}
